services:
  grocery-tracker:
    build: .
    image: grocery-tracker:local
    container_name: grocery-tracker
    restart: unless-stopped
    environment:
      - DATABASE_URL=file:/data/prices.db
      - NODE_ENV=production
      - FLIPP_POSTAL_CODE=N2B3J1
      - JWT_SECRET=GaGFkx2ay7@aNyzKx$3INAykLZ7xb12
    volumes:
      - db-data:/data
    networks:
      - apps
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:7800/api/stores"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  db-data:
    driver: local

networks:
  apps:
    external: true
